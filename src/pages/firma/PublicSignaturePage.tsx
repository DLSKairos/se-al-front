/**
 * Ruta pública: /firma/:token
 *
 * Pantalla de firma electrónica para firmantes externos.
 * No requiere autenticación JWT de SEÑAL — el token de firma es la credencial.
 *
 * STUB — implementación completa pendiente (agente de UI).
 */
export default function PublicSignaturePage() {
  return (
    <div
      className="min-h-screen flex items-center justify-center"
      style={{ background: '#0C1624' }}
    >
      <p style={{ color: '#F0F4F8', fontFamily: 'DM Sans, sans-serif' }}>
        PublicSignaturePage — en construcción
      </p>
    </div>
  )
}
